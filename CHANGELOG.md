# 更新记录

## 0.3.3

- 新增面向“词迹 CETTrace”的只读课程占用数据桥，词迹可自动避开实际上课时段。
- 数据桥只公开课程开始与结束时间，课程名、教师、教室、记录和其他个人信息均不对外提供。
- 查询同时校验词迹包名、调用 UID 和固定签名证书摘要；其他应用即使声明普通权限也无法读取。
- 课程、学期、例外、节假日或调休发生变化后会通知词迹自动刷新，无需手动导出或同步。
- 课程占用继续由课迹统一排课引擎计算，支持单双周、指定周、临时取消、换课和调休映射。

## 0.3.2

- 调休工作日卡片增加“选择对应课表”，可以把 A 日快捷改为按 B 日课表上课。
- 提供 A 日前后 7 天内工作日快捷选项，也可直接输入学期内任意 B 日。
- B 日的课程、教室、单双周和指定周规则会映射到 A 日，实际上课与提醒时间仍使用 A 日。
- 用户选择作为独立本地覆盖保存，国家节假日数据源再次同步时不会被覆盖。
- 已选日期可以随时更换，或一键“恢复来源规则”。
- 放假规则不允许误改为补课日；无效日期、同日复制和学期外日期会被拒绝。

## 0.3.1

- 默认接入公开 GitHub Release 更新通道，点击“检查并更新”即可在应用内查询和下载，无需登录 GitHub。
- 旧版本保存的空更新地址会自动回退到 GitHub 默认通道，不需要重新配置。
- 安全支持 GitHub Release 最多 5 次 HTTPS 重定向，拒绝降级到 HTTP、带凭据地址和异常跳转。
- 下载使用临时文件，失败时自动清理；成功后继续验证签名清单、SHA-256、包名、版本号及当前安装签名。
- 设置页仍可替换自定义 HTTPS 签名清单，并可一键恢复 GitHub 默认通道。
- Android 系统安装确认继续保留，不使用 root、静默安装或安全绕过。

## 0.3.0

- 默认开启国家法定节假日自动调整，内置 2026 年国务院办公厅公布日期，首次启动和离线时同样生效。
- 放假日会自动暂停当天课程，并同步影响首页、周总览、桌面组件、早课闹钟与课程提醒，无需逐条确认。
- 调休上班日会明确标注；国家通知未说明学校补哪天课时不会擅自复制课程。
- 支持在设置中替换可信的 HTTPS JSON 数据源；来源提供 `FOLLOW_DATE` 时可自动按指定日期课表补课。
- 每 12 小时在联网且电量不低时检查更新，重复启动不会重复联网；规则变更后自动重排提醒。
- 增加数据源大小、数量、日期、重复项和 HTTPS 校验，并防止来源切换期间旧请求覆盖新规则。
- 本版暂不接入学校校历，后续可直接沿用现有数据源协议扩展。

## 0.2.9

- 支持从系统文件选择器选择自定义照片作为全局应用背景，无需存储权限。
- 背景图按屏幕尺寸解码以控制内存占用，并提供清晰、平衡、柔和三档可读性遮罩。
- 增加默认、海蓝、青绿、紫罗兰、玫红、暖橙主题色以及自定义十六进制颜色。
- 自定义主题色自动生成适配浅色与深色模式的主要、次要及容器颜色。
- 系统动态配色与自定义主题色优先级明确，可随时恢复跟随系统壁纸。

## 0.2.8

- 重做学期设置的信息层级，当前学期、教学周校准、切换和新建不再挤在同一表单。
- 当前学期卡片直接显示教学周状态、第一周周一、总周数和课程数量。
- 支持编辑学期名称、周次锚点和教学周数；新学期自动建议名称与起始周一。
- 学期归档增加二次确认，并支持从已归档列表恢复并切换。
- 切换、新建、编辑、归档或恢复学期后会立即重新安排课程提醒。
- 修复旧版学期迁移可能在重启后覆盖用户手动校准结果的问题。

## 0.2.7

- PDF 渲染阶段显示页数进度；中转站响应阶段显示已返回字节数，并在服务器提供总长度时显示确定进度。
- 导入和确认写入使用互斥保护，卡顿期间重复点击不会启动并行任务。
- 使用 PDF SHA-256 指纹阻止同一文件生成重复草稿或重复写入。
- 导入只写入用户当前选择的学期；模型返回的学期日期不再自动创建或切换学期。
- 修复旧版本把安装当天当作第一教学周的问题；2026-09-18 正确显示为第 3 周。
- 学期管理增加“今天是第几周”校准入口。

## 0.2.6

- 课程与自学项目的历史记录支持点击查看。
- 记录详情展示摘要、事件时间线、来源、原文和完整性状态。
- 增加独立的 ChatGPT 课堂 JSON 导入框，支持“导入并继续”和“导入并结束”。
- 明确区分手动过程记录、人工摘要、普通聊天原文和结构化 JSON。
- 设置页增加自愿支持入口和本地赞赏码保存功能。
- 设置页增加 GitHub 源码、版本下载与问题反馈入口。

## 0.2.5

- 压缩 HyperOS 实时状态展示，移除静态剩余分钟和多余进度元素。
- 展开通知优先展示教室与准确起止时间。
- 课程支持单独设置超级岛简称。

## 0.2.3

- 校准课前倒计时与“上课中”的切换时刻。
- 修复提前时间设置与实际出现时间不一致的问题。

## 0.2.2

- 修复关闭课堂记录页、返回课程详情后触控卡死的问题。

## 0.2.1

- 完成个人课表、国际校区作息、周总览、课程编辑、桌面组件和真机适配。

## 早期构建

`0.1.0`、`0.1.1`、`0.1.2`、`0.2.0` 与 `0.2.4` 仅作为 APK 历史归档；它们没有各自独立的完整源码标签，不建议日常使用。
